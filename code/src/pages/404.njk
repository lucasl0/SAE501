{% extends "layouts/front-end/base.njk" %}

{% block bubble_color %}noir{% endblock %}

{% block title %}404 - Page introuvable{% endblock %}

{% block head_tags %}
    <meta name="description" content="Page introuvable - Profitez d'un jeu de casse-briques pour patienter">
    <link rel="icon" href="public/images/wrench-screwdiver-favicon.svg" type="image/svg+xml">
{% endblock %}

{% block main %}
<section class="flex flex-col items-center justify-center min-h-screen bg-white text-gray-800 ">
  <h1 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900">Oups ! La page n'existe pas... 😅</h1>
  <p class="mb-6 text-lg text-gray-600">Mais tu peux te détendre avec un petit casse-brique 🎮</p>

  <div class="relative">
    <canvas 
      id="breakout" 
      width="800" 
      height="600" 
      class="rounded-2xl shadow-xl border-2 border-gray-200 bg-transparent"
      style="background: rgba(255, 255, 255, 0.05); backdrop-filter: blur(2px);"
    ></canvas>
  </div>

  <a href="/" class="mt-6 text-blue-600 underline hover:text-blue-800 transition font-medium">
    Retour à l'accueil
  </a>
</section>

<style>
    /* Cache uniquement la navigation qui devrait être sur la page d'erreur */
    /* Sans affecter les autres éléments */
    body > header nav,
    .site-header nav,
    [role="navigation"]:not(.hero-bubble):not([class*="bubble"]) {
        display: none !important;
    }
    
    /* Force la visibilité de la bulle hero */
    .hero-bubble {
        display: block !important;
        position: absolute !important;
        top: 20px !important;
        right: 20px !important;
        z-index: 1000 !important;
    }
    
    /* Style minimaliste pour le canvas */
    #breakout {
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        border: 1px solid rgba(0, 0, 0, 0.05);
    }
    
    /* Assurer que le fond reste blanc */
    body {
        background-color: white !important;
    }
</style>
{% endblock %}